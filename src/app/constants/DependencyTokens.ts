import {OpaqueToken} from "@angular/core";

export abstract class Parent {
    public observableContext: any;
};
export abstract class LibraryMetadata {
    public library: Object;
    public site: Object;
};